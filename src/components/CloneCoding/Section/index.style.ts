import styled from "styled-components";

export const S = {
  CardGrid: styled.div`
    margin-top: 1rem;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(30%, 1fr));
    gap: 1rem;
  `,
};
